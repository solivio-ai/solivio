// Ambient declarations for declaration-less marker packages used by modules.
declare module "server-only";
declare module "client-only";

// CSS side-effect imports inside module UI (the app build handles them).
declare module "*.css";
