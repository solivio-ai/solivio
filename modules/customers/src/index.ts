import { defineModule } from "@solivio/sdk";

export default defineModule({
  id: "customers",
  title: "Customers",
  version: "0.1.0",
  description: "Customers and customer requests: data, services, CSV import, and admin UI.",
});
