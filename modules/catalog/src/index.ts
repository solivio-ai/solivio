import { defineModule } from "@solivio/sdk";

export default defineModule({
  id: "catalog",
  title: "Catalog",
  version: "0.1.0",
  description:
    "Product catalog: products and prices, semantic + keyword search, embedding import, and admin UI.",
});
